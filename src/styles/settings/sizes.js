import { createGlobalStyle } from "styled-components";

export const Sizes = createGlobalStyle`
    :root {
        --spacing-small: 10px;
        --spacing-medium: 20px;
        --spacing-large: 40px;

        --radius: 4px;
    }
`